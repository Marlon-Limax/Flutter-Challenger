import 'package:flutter/material.dart';

class _CustomTextFieldState extends StatelessWidget {
  const _CustomTextFieldState({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}